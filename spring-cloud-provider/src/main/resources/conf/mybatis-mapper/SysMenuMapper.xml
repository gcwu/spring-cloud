<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.system.mapper.SysMenuMapper">
    <resultMap id="baseResult" type="com.common.entity.domain.SysMenu" >
    <id column="ID" jdbcType="INTEGER" property="id" />
    <result column="CODE" jdbcType="VARCHAR" property="code" />
    <result column="NAME" jdbcType="VARCHAR" property="name" />
    <result column="URL" jdbcType="VARCHAR" property="url" />
    <result column="PARENT_ID" jdbcType="INTEGER" property="parentId" />
    <result column="LEVEL" jdbcType="INTEGER" property="level" />
    <result column="SEQ" jdbcType="INTEGER" property="seq" />
    <result column="STATUS" jdbcType="VARCHAR" property="status" />
    <result column="REMARK" jdbcType="VARCHAR" property="remark" />
    <result column="CREATE_USER" jdbcType="VARCHAR" property="createUser" />
    <result column="CREATE_TIME" jdbcType="TIMESTAMP" property="createTime" />
    <result column="UPDETE_USER" jdbcType="VARCHAR" property="updeteUser" />
    <result column="UPDATE_TIME" jdbcType="TIMESTAMP" property="updateTime" />

    </resultMap>

    <sql id="table_columns">
            ID,
            CODE,
            NAME,
            URL,
            PARENT_ID,
            LEVEL,
            SEQ,
            STATUS,
            REMARK,
            CREATE_USER,
            CREATE_TIME,
            UPDETE_USER,
            UPDATE_TIME
    </sql>
    <sql id="entity_properties">
            #{id},
            #{code},
            #{name},
            #{url},
            #{parentId},
            #{level},
            #{seq},
            #{status},
            #{remark},
            #{createUser},
            #{createTime},
            #{updeteUser},
            #{updateTime}
    </sql>

    <!-- 使用like用法：columnName like concat('%',#{columnName},'%') -->
    <sql id="page_where">
        <trim prefix="where" prefixOverrides="and | or ">
			<if test="id != null">and ID = #{id}</if>
            <if test="code != null and code != ''">and CODE = #{code}</if>
            <if test="name != null and name != ''">and NAME = #{name}</if>
            <if test="url != null and url != ''">and URL = #{url}</if>
			<if test="parentId != null">and PARENT_ID = #{parentId}</if>
			<if test="level != null">and LEVEL = #{level}</if>
			<if test="seq != null">and SEQ = #{seq}</if>
            <if test="status != null and status != ''">and STATUS = #{status}</if>
            <if test="remark != null and remark != ''">and REMARK = #{remark}</if>
            <if test="createUser != null and createUser != ''">and CREATE_USER = #{createUser}</if>
			<if test="createTime != null">and CREATE_TIME = #{createTime}</if>
            <if test="updeteUser != null and updeteUser != ''">and UPDETE_USER = #{updeteUser}</if>
			<if test="updateTime != null">and UPDATE_TIME = #{updateTime}</if>
        </trim>
        <if test="page != null and page.sort!=null and page.sort!=''">
            order by ${page.sort} ${page.order}
        </if>
        <!-- <if test="page == null or page.sort == null or page.sort == ''">order by sort </if> -->
    </sql>

    <insert id="insert" parameterType="com.common.entity.domain.SysMenu">
        insert into sys_menu( <include refid="table_columns" /> )
        values ( <include refid="entity_properties" /> )
    </insert>

    <insert id="insertBatch" parameterType="java.util.List">
        insert into sys_menu( <include refid="table_columns" /> )
        values
        <foreach collection="list" item="item" index="index" separator=",">
            (
            #{item.id},
                    #{item.code},
                    #{item.name},
                    #{item.url},
                    #{item.parentId},
                    #{item.level},
                    #{item.seq},
                    #{item.status},
                    #{item.remark},
                    #{item.createUser},
                    #{item.createTime},
                    #{item.updeteUser},
                    #{item.updateTime}
        )
        </foreach>
    </insert>

    <delete id="delete" parameterType="String">
        delete from sys_menu
        where id = #{id}
    </delete>

    <update id="deleteBatch">
        update sys_menu set delete_flag = 1
        where id in
        <foreach collection="array" item="id" open="(" separator="," close=")">
        #{id}
        </foreach>
    </update>

    <update id="deleteLogic" parameterType="String">
        update sys_menu set delete_flag = 1
        where id = #{id}
    </update>

    <update id="update" parameterType="com.common.entity.domain.SysMenu">
        update sys_menu
        <trim prefix="set" suffixOverrides=",">
            <if test="code != null and code != ''">CODE = #{code},</if>
            <if test="name != null and name != ''">NAME = #{name},</if>
            <if test="url != null and url != ''">URL = #{url},</if>
			<if test="parentId != null">PARENT_ID = #{parentId},</if>
			<if test="level != null">LEVEL = #{level},</if>
			<if test="seq != null">SEQ = #{seq},</if>
            <if test="status != null and status != ''">STATUS = #{status},</if>
            <if test="remark != null and remark != ''">REMARK = #{remark},</if>
            <if test="createUser != null and createUser != ''">CREATE_USER = #{createUser},</if>
			<if test="createTime != null">CREATE_TIME = #{createTime},</if>
            <if test="updeteUser != null and updeteUser != ''">UPDETE_USER = #{updeteUser},</if>
			<if test="updateTime != null">UPDATE_TIME = #{updateTime},</if>
        </trim>
        <where>id = #{id}</where>
    </update>

    <select id="findAll" resultMap="baseResult">
        select <include refid="table_columns" />
        from sys_menu
    </select>

    <select id="findList" resultMap="baseResult">
        select <include refid="table_columns" />
        from sys_menu
        <include refid="page_where" />
    </select>

    <select id="getCount" resultType="int" >
        select count(id) from sys_menu
        <include refid="page_where" />
    </select>


    <select id="get" resultMap="baseResult" parameterType="String" >
        select <include refid="table_columns" />
        from sys_menu
        where id = #{id}
    </select>

    <!-- 其他自定义SQL -->
</mapper>
